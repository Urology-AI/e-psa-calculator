# Known VITE_-prefixed secrets shipped to the browser.
#
# Anything listed here is PUBLIC to anyone who loads the app. Listing a
# variable does not make it safe — it records an accepted, outstanding risk so
# that check-vite-env.sh can still fail the build on anything NEW.
#
# This file is intentionally EMPTY.
#
# It previously carried two entries, both now resolved:
#
#   VITE_TURSO_AUTH_TOKEN   — a Turso database credential inlined into every
#     build. Clinical session access moved behind the Entra-authenticated
#     epsa-turso-proxy Worker, and this app no longer talks to Turso at all
#     (frontend/src/services/tursoService.js deleted). ROTATE the old token:
#     it was public in every deployed bundle.
#
#   VITE_CLINICAL_ADMIN_PIN — injected at build time but read by nothing. The
#     staff manager it once gated was removed in commit a0becd5. Delete the
#     GitHub Actions secret; it protects nothing and its presence implies a
#     control that does not exist.
#
# Keep this file empty. Adding a line requires a deliberate decision and, in
# nearly every case, rotating the credential first.
